{{#each items as |item idx|}}
  <li class="typeahead-item {{if (eq idx selectionIdx) 'active'}}" {{action attrs.onItemClick item idx}}>
    {{yield (hash
      item=(component 'block-component' content=item)
    )}}
  </li>
{{/each}}
